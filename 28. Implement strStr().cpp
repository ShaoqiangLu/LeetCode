#include <string>
#include <vector>

using namespace std;

class Solution {
public:
  int strStr(string haystack, string needle) {}

private:
  vector<int> KMP(string needle) {}
};

int main() {
  int r = Solution().strStr("hello", "ll");
  return 0;
}