#include <string>
#include <vector>

using namespace std;

class Solution {
public:
  vector<string> generateParenthesis(int n) {
    for (int i = n; i > 0; i--) {
      f
    }
  }
  void insert(string &str, char c, int n) {
    for (int i = 0; i < n; i++)
      str += c;
  };

  int main() {
    vector<string> r = Solution().generateParenthesis(3);
    return 0;
  }